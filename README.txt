# Step 1: Create a virtual environment
python -m venv venv

# Step 2: Activate the virtual environment
# On Windows:
venv\Scripts\activate

# On macOS/Linux:
source venv/bin/activate

# Step 3: Install dependencies from requirements.txt
pip install -r requirements.txt

# Step 4: Run the application
python app.py


static folder contains stylesheet (css)
screens folder contains all of our screens for our app (.html basically)

app.py routes and runs our app 

python app.py 
-> in google http://127.0.0.1:5000/ put this in URL.
